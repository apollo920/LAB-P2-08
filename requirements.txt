# Manipulação de Modelos e Tokenizers
transformers>=4.36.0
accelerate>=0.25.0

# Quantização (Requisito: 4-bit nf4)
bitsandbytes>=0.41.1

# Eficiência de Parâmetros (Requisito: LoRA)
peft>=0.7.1

# Pipeline de Treinamento (Requisito: SFTTrainer)
trl>=0.7.4

# Gestão de Dados
datasets>=2.15.0
pandas>=2.0.0

# Backend de Computação
torch>=2.1.0

# Utilitários (Simulação OpenAI e JSONL)
openai>=1.3.0
python-dotenv>=1.0.0